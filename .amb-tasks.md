# Foundation Next Steps
1. Implement speculative-pair dual-load mode in `lmstudio.rs`: `ensure_pair_loaded` + `list_loaded_instances`
2. Thread `load_mode`/`draft_model_key` through `executor/mod.rs` clean-room and speculative-pair branches
3. Add load-mode selector to `assets/dashboard.html`
4. Verify clean-room baseline run 610 and abort flow
5. Run one live speculative-pair test with both models resident, capture LM Studio instance IDs and latency
6. Keep `ingest/` schema impact review modular for future `030_human_calibration` migration
